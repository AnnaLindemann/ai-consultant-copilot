# env files
.env
.env.local
.env.*.local

# node
node_modules/
npm-debug.log*

# build
dist/
build/

# logs
logs/
*.log

# Database backups. Kept on the machine that made them — a dump of a working
# database carries real client content and belongs in no repository.
backups/
*.dump
*.sql.gz

# Scratch space. Holds the local development mailbox (server/tmp/dev-mailbox),
# which stores verification, reset, and invitation links as files, and the LLM
# benchmark's results (server/tmp/benchmark).
tmp/

# OS
.DS_Store

# IDE
.vscode/
