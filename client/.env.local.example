# Client environment. Copy to `client/.env.local`:
#   cp client/.env.local.example client/.env.local

# Base URL of the backend API. Defaults to http://localhost:8787 in code if unset,
# so this only needs changing when the server runs elsewhere.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8787
